<UserControl x:Class="WpfApp1.Character"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             x:Name="root"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Name="textBlockNomFamille" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Text="{Binding NomCharacter, ElementName=root, UpdateSourceTrigger=PropertyChanged}"></TextBlock>

        <TextBlock Name="textBlockInfo" Grid.Row="1" Grid.ColumnSpan="2" FontWeight="ExtraBold" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding PrenomCharacter, ElementName=root, UpdateSourceTrigger=PropertyChanged}"></TextBlock>



    </Grid>
</UserControl>
